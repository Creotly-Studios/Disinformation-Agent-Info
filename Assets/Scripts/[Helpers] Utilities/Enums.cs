using UnityEngine;

public enum PatrolMode
{
    Idle,
    Walk,
    Interact
}

public enum SpeakerType
{
    Other,
    Player
}

public enum CharacterType
{
    NPC,
    Player,
    Villain
}

public enum EmotionState
{
    Calm,
    Angry,
    Neutral
}